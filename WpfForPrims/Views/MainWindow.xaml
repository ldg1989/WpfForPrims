<Window x:Class="WpfForPrims.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfForPrims.Views"
        mc:Ignorable="d"
          xmlns:prism="http://prismlibrary.com/"
  prism:ViewModelLocator.AutoWireViewModel="True"
        Title="XXXXXXXXXXX" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="80"></Setter>
                    <Setter Property="Height" Value="30"></Setter>
                </Style>
            </StackPanel.Resources>

            <Button Content="模块A" Command="{Binding ShowContentCmm}" CommandParameter="ViewA"></Button>
            <Button Content="模块B" Command="{Binding ShowContentCmm}" CommandParameter="ViewB"></Button>
            <Button Content="后退" Command="{Binding BackCmm}"  ></Button>


            <Button Content="打开会话框" Command="{Binding DialogCmm}" CommandParameter="ViewC"  ></Button>
            <Button Content="发布消息" Click="Button_Click" ></Button>

            <Button Content="订阅消息" Click="Button_Click_1" ></Button>

            <Button Content="取消订阅" Click="Button_Click_2" ></Button>

        </StackPanel>
        <!--Content="{Binding ShowContent}"  MVVM 写法
        prism:RegionManager.RegionName="ContentRegion" 指定区域 
        -->
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ContentRegion"></ContentControl>


    </Grid>
</Window>
